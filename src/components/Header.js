import React from 'react';

const Header = (props)=>{
        return(
            <div>
                <h1>This is the header section of the application</h1>
            </div>

        )
}


export default Header

